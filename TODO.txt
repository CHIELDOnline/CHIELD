php:
	sanitise sql parameters

python script:
	Why is it not iterating over all files?

Deploy:
	Should delete public files before moving
	Add option to skip R compile
	

Finish addData.js
	*Add other columns
	Add date added? (so we can do recently added)
		Add to contributor line in addData.js
	Network:
		Add button: warn if variable not in database?
	When grid is updated, update vis.js
	Graph id property must stay the same, so check that sending data to grid is not using id, but "label"
	TEST Fix: When pressing enter on the input, hide the autofill
	Add "no causal relation"
	Clear bibtex reference on load
	FIX: Why are two pull requests submitted?

Cookies:
	TEST Store temporary variable names
	TEST Store version number
	TEST	Check if current version is greater than stored version
	TEST		Clear cookies if so
	FIX: git version will update with app changes even if data hasn't changed.
		SO: Rely on text version?
		NO: let's play it on the safe side

Documents: 


Document:
	Update links table (see causal_links)
	Hide bibtex source under button.
	LOW If bibtex has an abstract field, display it.
	Display causal links graph vis.js (use general function)
	Links table:
		Find a way to change the column widths
		Add button to make change request via github


causal_links:
	FIX: Why is search not working properly? type "tem", but "temperature" does not appear.

Database:
	Fix short citation: need to de-tex-ify special characters (see num 84)

Link: 
	Create page for link, add documents that relate to it


Graphical display:
	Add node
	Expland node
	Find shortest path between two links?
		http://aprogrammerwrites.eu/?p=1391#.WlUQPUu3yRs
	Find all paths between two links?
	General function to display causal links given a list of links
		Plus option to focus/emphasise a particular variable


Website:
	Finish other pages

Contributors:
	Contributor page shows ranked list of contributors

Variables:
	Sort by alphabetical



Coding:
	Fix "tool use ~ imperative gestures"
	Add important people: Kirby, Hurford etc.


